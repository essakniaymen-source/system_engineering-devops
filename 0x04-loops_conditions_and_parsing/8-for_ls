#!/usr/bin/env bash
# list all files, printing names after the first dash when present

for f in *; do
    [ -e "$f" ] || continue

    if [[ "$f" == *-* ]]; then
        echo "${f#*-}"
    else
        echo "$f"
    fi
done
